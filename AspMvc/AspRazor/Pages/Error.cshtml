@page
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

@*<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">An error occurred while processing your request.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}*@

<div class="wrapper vh-100">
    <div class="align-items-center h-100 d-flex w-50 mx-auto">
        <div class="mx-auto text-center">
            <h1 class="display-1 m-0 font-weight-bolder text-muted" style="font-size:80px;">500</h1>
            <h1 class="mb-1 text-muted font-weight-bold">خطای سمت سرور!</h1>
            <h6 class="mb-3 text-muted">متاسفانه خطایی رخ داده است</h6>
            <a href="./index.html" class="btn btn-lg btn-primary px-5">برگشت به صفحه اصلی</a>
        </div>
    </div>
</div>